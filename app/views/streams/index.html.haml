%h1 Listing streams

%table
  %tr
    %th Name
    %th
    %th
    %th

  - @streams.each do |stream|
    %tr
      %td= stream.name
      %td= link_to 'Show', [stream.board, stream]
      %td= link_to 'Edit', edit_board_stream_path(stream.board, stream)
      %td= link_to 'Destroy', [stream.board, stream], :confirm => 'Are you sure?', :method => :delete

%br

= link_to 'New Stream', new_board_stream_path(params[:board_id])
