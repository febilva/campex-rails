var current_step = <%= current_user.registration_form.current_step %>;
for(var i=0; i<= current_step; ++i)
{
  $("#side_menu").find(".menu.item[data-position='" + i + "']").removeClass("disabled");
}
